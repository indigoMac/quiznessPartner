[tool.black]
line-length = 88
target-version = ["py312"]
extend-exclude = "migrations/versions"

[tool.isort]
# Without this, isort's default import style disagrees with black and the two
# tools undo each other's work every time `make format` runs.
profile = "black"
line_length = 88
skip_glob = ["migrations/versions/*"]
